# P04.2 — Mach-O + Binary Capabilities

**Subphase**: P04.2  
**Status**: **PENDING**  
**Duration**: TBD  
**Endpoint**: EP-04.2  

---

## ENTRY CONDITIONS

| Condition | Status |
|-----------|--------|
| P04.1 completed | ✓ |
| Adapter base class available | ✓ |

---

## WORK

### 1. Additional Tool Adapters

Create `ios_reverse/adapters/core/`:
- `nm_adapter.py` — Symbol table analysis
- `otool_adapter.py` — Object file analysis
- `lipo_adapter.py` — Universal binary manipulation
- `strings_adapter.py` — String extraction

### 2. Mach-O Capabilities

Create `ios_reverse/capabilities/macho.py`:
- `MachoBasicCapability` — CAP-010
- `MachoSlicesCapability` — CAP-011
- `MachoLoadCommandsCapability` — CAP-012

### 3. Binary Capabilities

Create `ios_reverse/capabilities/binary.py`:
- `BinaryImportsCapability` — CAP-020
- `BinaryExportsCapability` — CAP-021
- `BinarySymbolsCapability` — CAP-022
- `BinaryStringsCapability` — CAP-023

### 4. Tests

Create `tests/test_capabilities_macho.py`
Create `tests/test_capabilities_binary.py`

---

## CAPABILITIES

### CAP-010: macho.basic

```yaml
id: macho.basic
version: 1.0.0
domain: mach_o
name: Mach-O Basic Analysis

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      magic: string
      cpu_type: string
      cpu_subtype: string
      file_type: string
      ncmds: int
```

### CAP-011: macho.slices

```yaml
id: macho.slices
version: 1.0.0
domain: mach_o
name: Mach-O Slice Analysis

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      slices: array
      architectures: array
      is_fat: bool
```

### CAP-012: macho.load_commands

```yaml
id: macho.load_commands
version: 1.0.0
domain: mach_o
name: Mach-O Load Commands

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      commands: array
      dylibs: array
      uuid: string
```

### CAP-020: binary.imports

```yaml
id: binary.imports
version: 1.0.0
domain: binary
name: Import Analysis

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      imports: array
      libraries: array
```

### CAP-021: binary.exports

```yaml
id: binary.exports
version: 1.0.0
domain: binary
name: Export Analysis

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      exports: array
      symbol_count: int
```

### CAP-022: binary.symbols

```yaml
id: binary.symbols
version: 1.0.0
domain: binary
name: Symbol Table

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      symbols: array
      defined_count: int
      undefined_count: int
```

### CAP-023: binary.strings

```yaml
id: binary.strings
version: 1.0.0
domain: binary
name: String Extraction

inputs:
  required:
    - name: artifact_path
      type: path
  optional:
    - name: min_length
      type: int
      default: 4

output:
  success:
    metadata:
      strings: array
      count: int
```

---

## TESTS

| Test | Capability |
|------|-----------|
| T01 | macho.basic |
| T02 | macho.slices |
| T03 | macho.load_commands |
| T04 | binary.imports |
| T05 | binary.exports |
| T06 | binary.symbols |
| T07 | binary.strings |

---

## EVIDENCE

*(To be populated during implementation)*

---

## QUALITY GATE

| Criterion | Result |
|-----------|--------|
| CAP-010 implemented | PENDING |
| CAP-011 implemented | PENDING |
| CAP-012 implemented | PENDING |
| CAP-020 implemented | PENDING |
| CAP-021 implemented | PENDING |
| CAP-022 implemented | PENDING |
| CAP-023 implemented | PENDING |
| All tests pass | PENDING |

---

## NEXT

After P04.2 completion → P04.3 Objective-C + Swift

---

*P04.2 pending implementation.*
