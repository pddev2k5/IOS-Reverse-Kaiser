# P04.3 — Objective-C + Swift Capabilities

**Subphase**: P04.3  
**Status**: **PENDING**  
**Duration**: TBD  
**Endpoint**: EP-04.3  

---

## ENTRY CONDITIONS

| Condition | Status |
|-----------|--------|
| P04.2 completed | PENDING |

---

## CAPABILITIES

### CAP-030: objc.metadata

```yaml
id: objc.metadata
version: 1.0.0
domain: objc
name: Objective-C Class Metadata

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      classes: array
      protocols: array
      categories: array
```

### CAP-031: objc.deep_metadata

```yaml
id: objc.deep_metadata
version: 1.0.0
domain: objc
name: Deep Objective-C Analysis

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      classes: array
      methods: array
      ivars: array
```

### CAP-032: swift.metadata

```yaml
id: swift.metadata
version: 1.0.0
domain: swift
name: Swift Type Metadata

inputs:
  required:
    - name: artifact_path
      type: path

output:
  success:
    metadata:
      types: array
      protocols: array
```

### CAP-034: swift.demangle

```yaml
id: swift.demangle
version: 1.0.0
domain: swift
name: Swift Symbol Demangling

inputs:
  required:
    - name: symbol
      type: string

output:
  success:
    metadata:
      demangled: string
      module: string
      type: string
```

---

## QUALITY GATE

| Criterion | Result |
|-----------|--------|
| CAP-030 implemented | PENDING |
| CAP-031 implemented | PENDING |
| CAP-032 implemented | PENDING |
| CAP-034 implemented | PENDING |
| All tests pass | PENDING |

---

## NEXT

After P04.3 completion → P04.4 Frameworks + dylibs + extensions

---

*P04.3 pending implementation.*
